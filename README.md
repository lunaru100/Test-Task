# Test-Task

To run the project you need:
- python (Pydantic,FastAPI
- react + vite

I completed both frontend and backend althought the backend filters that were required filter every currency so if you fetch like 15 currencies you may not get any results
Limitations: CoinGecko API does not allow too many requests, also wait time for coins to show up in frontend may be long if you fetch many coins on backend

